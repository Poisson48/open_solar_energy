# Legacy web UI (pré-QML)

Archive de l’ancienne interface HTML/JS embarquée dans WebEngine/WebView.
L’application native (AppImage / APK) est désormais 100 % QML + C++.

Ne pas réintégrer dans le build Qt.
